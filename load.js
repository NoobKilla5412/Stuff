// @ts-check

define(async function (req, exports, module, args) {
  // const { createElement } = await req("./js/HTMLUtils");
  // /**
  //  * @type {HTMLFormElement}
  //  */
  // const form = createElement("form", true);
  // const input = form.appendChild(document.createElement("input"));
  // input.focus();
  // /**
  //  * @param {string} url
  //  */
  // function load(url) {
  //   open(joinPath(currentImportPath, "..", `index.html?file=${encodeURIComponent(url)}`));
  // }
  // let url = prompt("Enter a file");
  // if (url) load(url);
  // close();
  // form.addEventListener("submit", (e) => {
  //   e.preventDefault();
  //   if (input.value) {
  //     load(url)
  //     input.value = "";
  //   }
  // });
});
